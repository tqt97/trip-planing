# Home fallback (database Trip home is source of truth after login)
HOME_NAME=Hotel Trường An Hotel
HOME_LAT=11.9370985
HOME_LNG=108.4220004

# Optional routing. App still works with Dalat fallback ETA without this key.
OPENROUTESERVICE_API_KEY=
ORS_BASE_URL=https://api.heigit.org

# Supabase collaboration (required for v2 multi-user)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
DEFAULT_TRIP_SLUG=dalat-2026

# Local seed only. NEVER expose in browser. Prefer the new sb_secret_ key.
SUPABASE_SECRET_KEY=
# Legacy fallback only:
SUPABASE_SERVICE_ROLE_KEY=
