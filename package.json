{
  "name": "dalat-nearby-planner",
  "version": "2.7.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "monkey": "node scripts/monkey.mjs",
    "quality": "npm run lint && npm test && npm run ui && npm run perf && npm run build && npm run smoke && npm run monkey",
    "dev": "node scripts/dev.mjs",
    "ui": "node scripts/ui-check.mjs",
    "perf": "node scripts/perf-check.mjs",
    "db:seed": "node scripts/db-seed.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
